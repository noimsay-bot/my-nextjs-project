# components/AGENTS.md

## 규칙
- 기존 디자인 톤과 간격 체계를 유지한다.
- 작은 기능 추가 때문에 공통 컴포넌트 전체를 갈아엎지 않는다.
- props 타입을 명확히 유지한다.
- 모바일 깨짐과 긴 텍스트 문제를 항상 확인한다.
- 관리자용 복잡도는 관리자 화면에만 한정한다.
- 이미 존재하는 공통 스타일 패턴이 있으면 먼저 재사용한다.
- UI 변경 시 기존 화면 흐름을 크게 바꾸지 않는다.
